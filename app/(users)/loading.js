const Loading = () => {
  return (
    // Or a custom loading skeleton component
    <div>
        <h1>Root Loading show for (user) folder inside all page.jsx</h1>
    <p>loading...</p>
    </div>
  );
};

export default Loading;
